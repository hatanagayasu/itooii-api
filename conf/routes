# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET     /websocket                  controllers.DispatchController.websocket()

GET     /*path                      controllers.DispatchController.dispatch(path)
DELETE  /*path                      controllers.DispatchController.dispatch(path)
HEAD    /*path                      controllers.DispatchController.dispatch(path)
POST    /*path                      controllers.DispatchController.dispatch(path)
PUT     /*path                      controllers.DispatchController.dispatch(path)
